# Anthropic API key — get one at https://console.anthropic.com
# This key stays server-side; never expose it in frontend code
ANTHROPIC_API_KEY=

# Streamlit server port (optional, defaults to 8501)
# STREAMLIT_SERVER_PORT=8501
